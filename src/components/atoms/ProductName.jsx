export default function ProductName({ name }) {
  return (
    <h4
      style={{
        fontSize: "20px",
        fontWeight: "600",
        color: "#333",
        margin: "10px 0",
      }}
    >
      {name}
    </h4>
  );
}
